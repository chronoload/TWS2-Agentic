services:
  ts2:
    build: .
    ports:
      - "80:80"
    environment:
      - PORT=80
      - TS2_WORKSPACE=/data
    volumes:
      - ts2_data:/data
    restart: unless-stopped

volumes:
  ts2_data:
